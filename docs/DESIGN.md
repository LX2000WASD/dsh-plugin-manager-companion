# dsh-plugin-manager-companion · 设计定稿

> 本文件是新仓库的**唯一设计权威**。所有实现决策以本文为准，偏离必须先改本文。
> 前身：`dsh-web-plugin-manager`（0.6.3，已停止维护，README 将注明由本仓库接续）。
> 官方基线：DSH `>= 0.1.6-alpha.2`。

## 0. 一句话定位

**官方插件管理器的伴生补强**：官方管"装什么、开什么"，我们管"装得对不对、环境健不健康、整个机器怎么管"。

## 1. 与官方的分工（不可越界）

| 能力 | 官方 | 我们 |
|---|---|---|
| 组合包启停 / 行级开关 | ✅ 已做 | ✗ **绝不做** |
| bundle 安装 / 卸载 | ✅ 已做 | ✗ **绝不做**（只在安装前插质量门）|
| 插件配置页托管 | ✅ 提供 slot | ✅ **作为注册方**接入 |
| 安装前静态质量门 | ✗ 不做 | ✅ **做** |
| 深度环境诊断 | ✗ 不做 | ✅ **做**（核心）|
| 多 profile 环境管理 | ✗ 明说"不能改另一个 profile" | ✅ **做** |
| 插件市场 | ✗ 无概念 | ✅ 做（不主打）|
| skill / agent 预设 | ✗ 明说"仍是文件操作" | ✅ **做** |

**硬约束**：所有当前 profile 的写操作走官方 `ctx.remote.pluginManager`；跨 profile 走官方 `./operations` 的 `runPluginCommand`。**我们不自己写 `cordis.patch.yml`，不自己调 pnpm。**

## 2. 四项官方明文空白（我们的作业面）

官方 README 原文（`boot/plugin-manager`、`client/ui-plugin-manager`、`client/ui-settings-plugin-inventory`）：

1. "The manager cannot ... **change another profile**, or **edit an agent preset's composition**"
2. "**Only bundles are managed** — ... loading plain plugin modules stays a file operation"
3. "**No version picker** — the page neither lists registry versions nor offers upgrades"
4. "enable/disable controls that write a custom preset's own composition file are **deliberate follow-up work**"

## 3. 页面结构

### 3.1 一级设置页（`settings.section`）

官方 order 布局：`general=0, models=10, plugins=15, agent-presets=20, archived-sessions=25`

| id | order | 名称 | 内容 |
|---|---|---|---|
| `marketplace` | 16 | 插件市场 | 索引浏览、搜索、分类、安装（走官方）|
| `console` | 17 | **环境控制台** | **健康检查 + 环境管理合一**（见 §4）|
| `kinds` | 22 | 技能与预设 | SKILL.md / agent.cordis.yml 直装 |

### 3.2 官方插件页内的注册（"点开即可管理"）

按官方 `slot-contract.ts` 声明，**注册方与页面同生**：

| slot | key | 我们注册什么 |
|---|---|---|
| `plugins.item` | — | 我们自己的条目卡片（label 用字典）|
| `plugins.bundle.config` | `dsh-plugin-manager-companion` | 本插件在**官方插件页**的配置表单 |
| `plugins.row.config` | `<包名>#<行 id>` | 需要逐行配置的组件 |

**已实证**：`PluginManagerPage.tsx:966` 的 `configured = ledger.bundles.has(openPkg.name)`，用自己包名注册即渲染。

### 3.3 配置写入：不编辑配置文件

用官方 `ctx.settingsScope.bind({ namespace })` + `register()`（`SettingsProvider`），
用户在我们的表单里改，**由官方 settings 服务落盘**。我们不碰 YAML。

## 4. 环境控制台（合并页，本仓库的核心）

用户定位："类似 360 电脑管家"——**对本地 DSH 环境做深度、可视化的分析与管理**。

### 4.1 设计原则

- **只读优先**：默认只诊断、不修改；任何写操作都要明确点击 + 行内确认
- **证据可溯**：每条诊断都能点到源文件/行，不做黑盒判断
- **官方事实优先**：能用 `ctx.reflect`/`ctx.loader` 读到的，不靠源码正则猜

### 4.2 诊断维度（分层）

| 层 | 数据源 | 检测项 |
|---|---|---|
| L1 依赖 | 静态扫描（package.json + import 图）| 缺失依赖、未声明 import、官方包重复、peer 版本 |
| L2 组合 | 官方 `composeEntries` + patch 层栈 | 行 id 重复、被禁用依赖、遮蔽、孤立行 |
| L3 运行时 | `ctx.loader.entries()` + `ctx.reflect` | fiber 相位异常、pending 根因、失败原因、服务名冲突 |
| L4 一致性 | 官方 `readPluginInventory` vs 本地文件 | 声明未装、装未挂载、挂载未声明 |
| L5 生态 | 市场索引 | 更新可用、已知风险徽标、废弃包 |

### 4.3 分级处置

- **A 级（可安全自动修）**：重复行 id、官方包重复、被禁用依赖 → 一键修复
- **B 级（需确认）**：同名注册冲突、缺失依赖 → 建议 + 行内二次确认
- **C 级（只报告）**：循环依赖、peer 不满足、语义冲突 → 输出 + 溯源

### 4.4 环境管理（同一页面）

- 环境列表（运行状态、端口、进程）
- 启停（终端/后台，端口探测）
- 创建 / 重命名 / 删除（官方 profile 只读保护）
- 插件跨环境复制/转移
- 备份导出 / 差异对比 / 导入恢复

**写路径**：官方 `runPluginCommand({ profile, dir, installAnchor, cwd, home })` + `withFileLock`。
**恢复路径**：官方 `sanitizeProfile(binName, profileDir, bundles)`。

## 5. 质量门（安装前拦截）

```
1. inspect(spec)                              ← 官方 Remote
2. installBundle(spec, { enabled: false })    ← 官方 Remote（装但不激活）
3. 扫描已安装包（我们的质量门）                ← 独有
4. 不合格 → removeBundle(name)                ← 官方回滚
   合格   → setBundleEnabled(name, true)      ← 官方激活
```

零竞态、零自建 pnpm、零 patch 写入。官方 UI 自己就用 `enabled: false`（`manager-store.ts:561`）。

### 5.1 第一步：静态快筛（已实现）

上面那四步就是快筛：装但不激活 → 扫包 → 放行或回滚。它是**只读文件**的，成本 60–79ms，
能抓住"装进去就炸"的那一类：未声明 import、声明了没装、官方包被声明成普通 `dependencies`、
入口解析不到、bundle patch 行解析不到。

**它抓不到"只有真挂载才暴露"的那一类**（实测 3 种坏法只抓到 1 种），所以需要第二步。

### 5.2 第二步：试装（真实安装验证）

**目标**：在用户点"安装"时，先把候选包装进一个测试环境并**真的启动一次**，看它能不能被挂载。
实测对照（三种坏法注入真环境逐个 boot）：patch 行挂 import 不到的模块 → 静态门也抓到；
模块 `apply()` 启动期抛错 → 只有启动抓到；服务名冲突（占 `pluginManager`）→ 只有启动抓到。
**真实启动 3/3，静态门 1/3**。两者是接力不是替代：静态门 60–79ms 做前置快筛，试装做最终判定。

**验证形态（不得偏离）**：

- 官方**没有**"启动 + 验证 + 退出"模式（`apps/cli/src/args.ts` 的 `DshInvocation` 只有 profile / dump-config / plugin）。
- `--dump-config` 60–79ms 且**不加载任何 JS** → 抓不到 import/apply/服务冲突，只能当组合层快筛。
- 真验证 = 启动一次并读**官方自己的就绪信号**。两类环境各用各的信号，谁先出现算谁：
  · **headless 类**：启动且不给任务，519–558ms 真挂载整棵树，拆除 2ms；就绪 = stderr 只有一行
    `dsh: a task is required…`。
  · **含 web 层的环境**（官方默认模板建的、以及 GUI 自己那个环境）：缺任务法**不成立** —— 端口 3080 被 GUI
    占着会 EADDRINUSE，让开端口后树挂上了却以服务形态常驻、只能报"判不出来"。改跑服务形态：
    启动参数加 `--port 0 --no-open`（官方 CLI 只解析启动器自己的标志，其余原样交给树；官方自己的 e2e 与
    发布脚本就是这么跑的：`dsh web --no-open --host 127.0.0.1 --port 0`），**读 stdout** 的
    `dsh web: http://…` —— 该行在 Loader settle 之后才打印，官方注释写明它是给 supervisor 的就绪信号；
    读到即判 mounted 并立刻杀子进程。`--port 0` 由 OS 分配端口，永不与 GUI 抢口。
- **退出码不可用于判定**——健康环境也是 exit 1（官方以"缺任务"收场）。失败一律读 stderr：
  `plugin tree failed to load` + 44–52 行 cause 链；两类信号都没有 → "判不出来"，不许当通过。
- **绝不传任务文本**。实测 `env -u DEEPSEEK_API_KEY` + `stdin=/dev/null`：不需要 key、不调模型、
  无 session 落盘（只多一个 223 B 的 `cordis.yml`）。给任务就变成"真跑一轮 agent"，那是花用户的钱。

**受控对照四步（缺一步结论就站不住）**：

```
① 物化快照 → ② 基线启动 → ③ 装候选包 → ④ 二次启动
   ②失败 → 如实说"当前环境快照本身就起不来"，不赖候选包（并把这个发现升级成一条诊断）
   ②成功 ④失败 → 这才是"候选包导致的"，给出 stderr 根因链
   ②成功 ④成功 → 通过
```

②那一步多花 558ms，但把结论从"失败了"变成"**是它导致的**"。默认开启，可在设置里关。

**快照深度以启动为判据，不做预测**（2026-09-19 真机改定，取代原先的"层栈能否由安装锚点解析"谓词）：

安装方提供的层（`@deepseek-ai/dsh-base` / `dsh-web-app`）**不在 profile 的依赖里**（实测：web profile 的
`dsh.profile.bundles` 含这两项，`dependencies` 里没有），官方 `resolveBundleDir(anchor, profileDir)` 也解析不到它们
（只存在于 pnpm 虚拟 store；探针命令与原文见 §5.5）。因此旧谓词对**所有原装环境**都返回"真实快照"，
浅快照会变成永远走不到的分支——而浅快照真机又能挂载（526ms）。改为：

- 自动 = 先浅快照；②的判定为**明确失败**时才自动改用真实快照重新物化、重跑②；两次都不挂载才报
  "快照基线就起不来"，文案写明"浅快照与完整快照都试过"及浅快照为什么不够；
- 两种失败**形态不同、可区分**：浅快照缺依赖是启动器在挂载前 throw（`cannot resolve profile bundle "…"`），
  真实环境本来就坏是 `plugin tree failed to load` + cause 链。只认后者时升级永不触发（真机跑出来才发现分支是装饰）；
- 判定为"判不出来"（超时、无可识别特征）时**不升级**：那是"无法试装"，不是失败，也不许当通过；
- 结论里带**实际用了哪种深度**（`浅快照` / `真实快照（由浅快照升级：原因）`）。

真机证据：全官方层环境 + 自动 → 实际深度 = 浅快照、基线/候选都挂载（548ms）、结论通过；
层栈含 profile 自装层 → 浅快照基线 `cannot resolve profile bundle dsh-t50-linklayer` → 升级为真实快照后两条都挂载、结论通过。

**成本（全部本机实测，含口径）**：

| 步骤 | 耗时 | 磁盘 |
|---|---|---|
| 建测试环境（headless） | 34ms | 488 B / 12 KiB 块；**无 node_modules**（依赖由安装锚点解析）|
| 浅快照（只复制清单文件） | 毫秒 | KB 级 |
| 真实快照（`pnpm install --prefer-offline`） | 831ms 冷 / 59ms 热 | 看似 +9.3 MiB，但因 **pnpm 硬链接**（1243/1247 文件 nlink>1）store 增量为 **0**，**独占占盘仅 36 KiB** |
| 装候选包 | `link:` 33ms；registry 冷 store + 联网 1159ms、热 29ms | 冷 store 时 store +4.0 MiB |
| 启动验证 | 558ms（复跑 524ms），拆除 2ms | 块 I/O 读 0 写 0 |
| 浅快照一轮（真机，全官方层） | 1139ms 总（两次验证 526 / 530ms） | 浅快照 3053 B 表面 / 40 KiB 块；真实环境本身 549 B |
| **一轮完整试装** | **≈0.65s**（冷 store + 联网下载候选包 ≈2.3s） | —— |

**内存峰值 ≈161 MiB/次验证启动**（maxrss）。磁盘几乎不要钱，内存才是真实成本，
UI 必须如实告知——它是"点一下会发生什么"的一部分。

**试装会真的执行第三方代码**：候选包被真实安装进测试环境，带 `postinstall` 的包会在用户机器上真的跑。
接受这条风险是本功能成立的前提，因此必须**显式开启 + 明确告知**（默认关闭）。
让第三方代码不在真实环境里执行需要自建沙盒，列入路线图（§10），不在本轮范围。

**失败的三种结论各有措辞**，不得混：`快照基线就起不来`（不是候选包的问题）/ `候选包导致挂载失败`（给根因链）/
`无法试装`（例如禁用联网且本地 store 没有该包）——最后一种**不能算通过**。

### 5.3 试装的设置项

放在「环境控制台 → 设置」子页（字段多且需要解释，塞进官方 bundle 表单会很难看；
两处共用同一个 settings store，所以在官方插件页也能看到这些键）。

| 选项 | 默认 | 说明 |
|---|---|---|
| 试装总开关 | **关** | 打开时明确告知：会在你机器上真实安装候选包并执行它的安装脚本 |
| 快照深度 | 自动 | 自动 = 先浅快照，基线**明确失败**才升级为真实快照并重跑基线（见 §5.2）；结论里写明实际深度。可显式强制浅快照 / 真实快照 |
| 试装前做基线启动 | 开 | 见 §5.2 四步里的②；关掉省 558ms，但失败时说不出是谁的问题 |
| 允许联网拉取候选包 | 开 | 关掉则只用本地 store，冷包直接判"无法试装"（如实说，不假装通过）|
| 试装失败时的行为 | 阻止安装 | 另一档：只警告，由用户决定 |
| 自动清理测试环境 | **开 / 14 天** | 时间可改；可整体关掉自动删除 |
| 最多保留测试环境数 | 不限 | 不设硬上限，但设置页显示**总占地** |

### 5.4 测试环境与状态同步

**命名与归属**：`<当前真实环境名>-dpmc`，一个真实环境对应一个测试环境（保真需要）。
它在环境列表里必须一眼可辨、禁止重命名，且不得被当成真实环境操作。

**同步原则：不订阅变化，使用时即时物化。**

用户可能在终端里跑官方 `dsh plugin add`、也可能直接改本地文件——这些外部改动**不可能被通知**
（官方没有这类事件钩子）。但**不需要被通知**：只要我们在"要用它的那一刻"重新读一遍磁盘事实，
看到的就是最终状态。订阅需要钩子，拉取只需要能读文件系统。

机制四步：

1. **环境指纹**（五元组，**全部读盘，不读我们自己的内存台账**）：`package.json` 内容 hash +
   `pnpm-lock.yaml` hash + `cordis.patch.yml` hash + 官方 `listBundles()` 的层栈 + 依赖清单。
   为什么不读台账：用户在终端里的改动不会经过我们，台账必然过期。
2. **每次试装前**：算真实环境指纹 → 与测试环境记录的指纹比对 → 不一致就重新物化。
   代价可接受，因为试装是事件驱动的（只在用户点安装时发生），不是周期任务。
3. **结果带快照标识**：指纹 + 时间戳，UI 写一句事实"基于 N 分钟前的环境快照"。
4. **试装后再算一次指纹**，不一致就如实说"试装期间环境又变过，这个结论可能不适用"。

残留的是"快照瞬间 → 试装完成"这几秒的竞态，**不可消除**（真实世界就是会变），
能做的只有第 4 步的如实标注——不说"没问题"，只说"基于什么、结论是什么、还缺什么"。

**测试环境永不是真值来源**：不向上同步、不参与诊断结论、删除必须安全（正在运行则先拒或先停）。

**清理**：默认开启自动清理、保留 14 天，用户可改天数或关掉。删除前只针对形如 `<名>-dpmc` 且确认
属于我们的环境；真实环境被改名/删除后遗留的孤儿测试环境同样纳入清理范围。删除动作记日志（删了哪个、为什么）。

### 5.5 插件升级（档三）

**范围（用户 2026-09-19 定）**：只处理"这套软件本身"——① 官方运行时（全局安装的那份）② profile 依赖里的官方实验包 ③ 本插件自身。
**第三方插件不进「关于」页**；它们的升级入口是官方插件页里该包自己的页面 + 市场页卡片。

**动作落点（已核官方源码，只有这三个座位）**：`plugins.item`（list，只服务"官方"那一组）/
`plugins.bundle.config`（keyed by 包名 → 该组合包**自己的页面**里的配置区）/ `plugins.row.config`（keyed `<包名>#<行 id>`）。
官方插件页的**卸载按钮与启用开关在 `DetailTop` 的 actions 里，没有槽位** —— 所以"加在删除旁边"做不到，**禁止 DOM 注入**。

**检查时机 = 进入即查 + TTL + 手动常驻，不做后台轮询**。"每天一次"的语义是"下次进入时若距上次成功检查超过 24h 就查"，
不是后台定时承诺。设置三项：自动检查开关（默认开）/ 检查间隔（每次打开 · 6 小时 · 每天 · 仅手动）/ npm registry 地址（默认官方，可填镜像）。
失败也记时间戳（默认 1 小时内不自动重试），手动按钮永远可用。**事实来源优先级**：市场索引里已有的版本字段（零网络）→ npm dist-tags。

**四态，各自可辨（防"靠缺席传达"）**：有更新 → 给升级入口；已检查、无更新 → 不在插件页显示（"已是最新"的口径在市场页/关于页）；
检查失败 → 必须显示"查不到：<原因>"+ 重试，**绝不显示"已是最新"**；不可升级（安装方提供的层）→ 说明 + 命令，不给按钮。

**版本选择**：列出该包**全部 dist-tags**（tag 名 + 版本 + 这条线是什么），默认高亮与当前版本**同线**的最新；
升到别的线必须明说"会切到 X 线"；所选版本等于当前版本则不给可点按钮。

**执行**：一律走官方通道（`runPluginCommand(['add','<name>@<版本>'])`），绝不自己调 pnpm、绝不写 `cordis.patch.yml`。
金丝雀复用 §5.2 的试装引擎（在 `<环境>-dpmc` 里先装新版本并启动一次）；**`cannot-trial` 是"没验证"，不是"通过"**，未通过就不动真实环境。
自我升级走独立 job（正在运行的就是旧代码），生效时机沿用官方口径（"已安装，下次启动后加载。"）。

**回收**：注册集合随已装集合实时对账（`register()` 的 disposer 按需释放；官方台账是 live 的，撤掉 key 当场消失），不留孤儿 key；
金丝雀测试环境**用完即删**并写清理日志（它不是用户资产，不走 14 天）；检查缓存按包失效；job 记录只留内存。

## 6. 删除清单（相对旧仓库）

| 删除 | 行数 | 理由 |
|---|---|---|
| `PluginCatalogTab.tsx` | 593 | 官方侧栏 Plugins 页已覆盖且更好 |
| `PluginManagerSettingsTab.tsx` | 766 | 官方安装对话框已覆盖 |
| 遮蔽注册（`settings.plugins.tab` id='all'）| — | 官方主动降级为只读列表 |
| `patch.ts` | 511 | 写权交还官方 |
| `live.ts` | 411 | 官方换用 `@deepseek-ai/dsh-hmr`，旧的绕死锁方案已过时 |
| `childproc.ts` | 222 | 改用官方 `./operations` |
| 27 个 REST op + job 系统 | — | 改用官方 Remote |
| `tsdown.config.ts` | 19 | 死配置 |
| **`cordis.patch.yml` 的 `id: plugin-manager`** | — | **P0 根因**（与官方新行撞 id）|

## 7. 已知必须修的旧代码缺陷（参考不复制）

调研中已实证的旧仓库问题，新仓库必须避免：

1. **P0-A**：bundle patch 行 id `plugin-manager` 与官方新行撞车 → `duplicate loader entry id`，profile 无法启动
2. **P0-B**：服务名 `ctx.pluginManager` 与官方同名 → `service has been registered`，profile 无法启动
3. **fiber 相位映射错**：旧 `phaseOf` 把 `4` 映射为 `null`（官方 `4=DISPOSED`）、`5` 落 `unloading`（官方 `5=UNLOADING`），但官方 `PluginFiberPhase` 只有 5 个值 + null，**缺 `disposed` 语义**
4. **tsconfig.host.json include 缺 `marketplaceMerge.ts`**：靠传递依赖侥幸编译
5. **`CONTEXT.md` 过期**：声称"未 push"，实测已同步

## 8. 官方标准对齐清单（"绝不多做轮子"）

| 维度 | 官方标准 | 我们 |
|---|---|---|
| 一级入口 | `ctx.slots.inject('settings.section')` | ✅ |
| 样式 | `@deepseek-ai/dsh-client-ui-primitives` | ✅ 不自造组件 |
| i18n | `ctx.locale.register(NS,{zh,en})` | ✅ |
| 状态 | `@deepseek-ai/dsh-client-store` `createSnapshotStore` | ✅ |
| 跨端 | `ctx.remote.<ns>.<method>` + `$on` | ✅ 替代自建 REST |
| 配置 | `ctx.settingsScope.bind()` | ✅ 不碰 YAML |
| 文件锁 | `withFileLock` | ✅ |
| 原子写 | `writeFileAtomic` | ✅ |
| 客户端产物 | `dsh.client` + lazy-CJS factory | ✅ |
| externals | 严格等于官方 `PLATFORM_MODULES`（**9 项含 dockkit**）| ✅ |

## 9. 兼容与迁移

- 只支持 **DSH >= 0.1.6-alpha.2**
- 旧仓库不归档，README 注明由本仓库接续
- 包名不同（`dsh-plugin-manager-companion`），可与旧包共存一段时间
- 迁移：卸载旧包 → 装新包（旧包的 `plugin-manager` 行需清理，见 §7.1）

## 10. 待办（实施阶段）

- [ ] 骨架与构建（package.json / tsconfig ×3 / tsdown.client.config.ts / cordis.patch.yml）
- [ ] 契约测试（client-boot：平台表 + slot 注册面 + 字典键位）
- [ ] 官方 Remote 适配层
- [ ] 环境控制台（诊断引擎 + 环境管理）
- [ ] 质量门包装器
- [ ] 市场搬迁（参考重写，不复制）
- [ ] 技能与预设
- [ ] 官方插件页内的注册（`plugins.item` / `plugins.bundle.config`）
## 11. 传输层决策（实施期补充，取代第 8 节该行的笼统表述）

第 8 节写的"跨端：ctx.remote.<ns>.<method> 替代自建 REST"只对**官方能力**成立。
实测后的精确划分：

| 调用目标 | 通道 | 依据 |
|---|---|---|
| 官方能力（pluginManager.*、pluginInventory.*） | **官方 Typert Remote**，`ctx.remote.<ns>.<method>` | 官方在 `api-remotes` 里按 build-time value import 挂载；第三方可直接注入使用 |
| 本插件自有能力（诊断/环境/市场/技能/配置） | **自有 REST**，注册在 `ctx.webServer.register` | 见下 |

### 为什么自有能力不用官方 Remote

官方 `api-remotes` 的能力集合是**编译期 value import 固定的**（其 README 明说"增加能力需显式
/remote import 与 mount"）。第三方要自带 Remote 就必须用 `@deepseek-ai/dsh-typert-generator`
生成 `./typert` 与 `./remote` 产物；而该生成器在 npm 上的可用版本是 `0.0.1-rc.1`，
远落后于运行时 `0.1.6-alpha.2`。用版本错配的生成器产出运行时反射产物，是比 REST 大得多的风险。

`@deepseek-ai/dsh-host-webserver` 的 `WebRoute` / `WebServer.register` 是**官方公开路由 API**，
不涉及生成器。旧仓库已用它跑通完整链路，包括信任围栏。

### REST 契约（host 与 client 的唯一约定）

- 前缀：`/api2/companion`（**避开**旧仓库的 `/api2/plugin-manager`，两包可能短期共存）
- 只接受 `POST` + `content-type: application/json`
- 信任围栏：Host 必须是回环或白名单；`Origin` 存在时必须同源（防 CSRF/DNS-rebinding）
- 请求体上限分级（默认 1 MiB；备份导入 16 MiB）
- 响应信封：`{ ok: true, value: T }` 或 `{ ok: false, error: { code, message } }`
- 长操作（诊断全量、批量恢复、跨环境 pnpm）必须 job 化：POST 即返 `{ jobId }`，
  客户端轮询 `job` op。理由：HTTP 超时不可与服务端状态脱节。
## 12. UI 文案标准（用户反馈驱动，2026-09-19）

**用户反馈原文**：*"UI 上不要出现类似『诊断、多环境管理都在这一个入口里；写操作一律走官方通道』
这样的解释性段落……官方 DSH 也没有加入这些过于冗余的解释性注释段落在自己的 UI 中。"*

这条反馈是对的，而且它指向一个可判定的标准。

### 12.1 官方标尺（实测文案）

| 官方页面 | 它的 intro |
|---|---|
| 模型设置 | `填入各提供方的 API 密钥即可使用其模型。` |
| 插件设置 | `查看内置部署的插件列表` |

两句都很短，且只回答**"这里能做什么"**或**"这里有什么"**。
官方 UI 里**没有任何一句**解释通道、传输、服务归属或模块结构（grep 过 remote/channel/service/via 零命中）。

### 12.2 硬规则

**一级页面的 intro 最多一句，只描述"这里有什么"或"这里能做什么"。**

禁止出现（三类）：

1. **实现与通道**：`走官方通道` / `由官方 settings 服务落盘` / `不写 YAML` / `经 loader 应用`
2. **结构说明**：`都在这一个入口里` / `和「设置 → 某处」是同一份文档` / `插件本体在官方插件页管理`
3. **自夸**：`如实标注` / `每条发现都能展开证据` / `绝不静默`——做到了自然看得见，写出来是噪声

保留（这些是用户**需要**的）：

- 空态（`还没有通过本插件安装的技能或预设。`）
- 错误与失败原因（`环境不存在：xxx`）
- **用户即将做的动作的后果**（`关闭的层会在体检里跳过`、`卸载后需要重启才完全生效`）
- 加载态、字段标签、占位符、确认按钮文案

### 12.3 唯一判据

> **这句话删掉之后，用户还能不能完成任务？**
> 能 → 删。不能 → 它是必须的信息，保留（并尽量缩短）。

判据本身没问题，但它有个前提容易被跳过：**先看屏幕上已经显示了什么**。
如果这句话提供的信息已经由界面的其它部分提供（列表项、选择器状态、组头计数），
那么删掉它用户当然还能完成任务——**但也因此它本来就不该存在**。

2026-09-19 的教训：本节曾写着「五层诊断：依赖、组合、运行时、一致性、生态。保留前半（用户要认识这五个词）」。
这是错的——那五个层的名字**就在下面逐个显示着**，intro 只是把屏幕上的东西又念了一遍。

### 12.3.1 第二次反馈补充的四类（2026-09-19，用户逐条举例）

用户给了三个实例，它们共同指向四类必须删的文案：

| 类 | 实例 | 为什么删 | 正确形态 |
|---|---|---|---|
| **重复屏幕已有信息** | `五层诊断：依赖、组合、运行时、一致性、生态` | 层名就在下面显示着 | 删（或什么都不写）|
| **解释设计意图** | `默认诊断当前环境` | 页面打开就显示当前环境，选择器也可见，不言自明 | 删 |
| **状态写成句子** | `选中其他环境后本页只读` | 状态是**事实**，不是解释 | 换成选中时的「只读」标记 |
| **总结后的括注** | `同类发现已归并为 3 组（共 3 条，逐条证据一条不少）` | 前半是好的总结；括注里"共 3 条"是重复计数，"逐条证据一条不少"是纯解释 | 只留「同类发现已归并为 3 组」|

可复用的说法：**介绍性文字不是功能字段**。一个字段只有在"删掉它用户就做不成这件事"时才留；
而"讲我们怎么想的""把屏幕念一遍""保证我们知道得很多"都不属于这一类。
### 12.3.2 第三次反馈补充的两类（2026-09-19，用户逐条举例）

| 类 | 实例 | 为什么删 |
|---|---|---|
| **复述控件的当前值** | 选择器上方那行 `诊断目标：pm-ui-task48-other` | 选择器本身就把值显示在那里；再用一行文字写一遍是同一事实出现两次 |
| **控件内部再写一遍旁边标记已经说了的事** | 选择器选项标签里的 ` · 当前环境` 后缀 | 选择器**旁边**已有「非当前环境」标记；列表项里再拼一个后缀是把同一事实说两遍。**载体只留一个**：留控件旁的标记，删控件里的后缀 |
| **指路式引导** | `修改请到「环境」子页。` | 目标页就在同一排标签里可见，用户发现改不了时会自己点过去。**这是引导式交互，不是需要写出来的说明** |

**这条修订了我先前的一个判断**：我曾把 `修改请到「环境」子页。` 归为"可执行出路"而保留。
正确的切分是——**"出路"指的是给出具体命令、参数、文件名这类用户照做就能完成的信息**（例如 `dshpmc analyze --profile X`、
要改的文件与行号）；**而"去另一个页面"不算出路**，因为导航本来就摆在界面上。

**配套的界面要求**：删掉"指路"的前提是**目标入口在屏幕上可见且可点**。
所以在删「修改请到「环境」子页。」的同时，断言必须证明那个标签真的在（§12.5 的替代载体）。

同类检查的口径：凡是**同一屏里同一事实出现两次**的地方都要处理——包括"控件值 + 文字复述"与"状态句 + 标记"。
### 12.3.3 不允许用"没有标记"来表达状态（2026-09-19，用户第三次反馈的后续）

删掉选择框里的 ` · 当前环境` 后缀之后暴露了一个更深的问题：**"这个目标是不是当前环境"只剩"没有标记"一种表达**——
选中当前环境时界面上一片安静，用户只能从"没有那个标记"去推断。

这和我们一直在防的那一类是同一个家族：**把"没查"画成"没问题"**、以及 §12.5 的"删信息必须证明替代载体还在"。
共同的判据是——

> **一个事实要么在屏幕上被明确说出来，要么它就该被删掉；不能靠"缺席"来传达。**

所以：**「非当前环境」有标记，「当前环境」也要有标记**（用一个中性色标记，不是警告色——它不是问题）。
两边都有，才是"表现对齐"的实质含义：用户不需要比较"有还是没有"，直接读到状态。

反例边界（什么时候"缺席即状态"是可接受的）：**当缺席本身就是用户要看的那个量**的时候。
例如列表里"没有发现问题"就是结论本身；但如果某个标记的**存在**意味着"要注意"，那它的缺席就会被读成"没问题"，
而"没问题"是需要被证明的，不能靠没写。

### 12.4 护栏

标准要靠检查才立得住：`tests/ui-copy.test.mjs` 扫描 `src/client/locales.ts`，
对禁止词表做断言（同时覆盖 zh 与 en），并断言 intro 类文案不超过一句话的长度上限。

2026-09-19 补充：上面三类实例说明**词表 + 句长上限不够**——那三条当时全都能过检查。护栏要加三条：

1. **intro 不得枚举「界面上已渲染的元素名」**：`.intro` 里出现 3 个及以上**已渲染元素的名字**即判失败
   （直接抓住「依赖、组合、运行时、一致性、生态」这种把屏幕念一遍的写法）。

   ⚠ 这条最初被写成"出现 3 个及以上以 `、` 分隔的枚举项即失败"——**那是过钝的形态规则，会误杀合法枚举**。
   理由才是判据：**"把屏幕上已经显示的东西又念一遍"**。所以护栏要拿一份显式的「已渲染元素名」清单去比，
   清单每一项注明它渲染在哪里；光看顿号数量不算数。
   边界样本：`社区索引里的插件、技能与预设。` 若市场页上确实渲染了这三类（分类/标签），它就是重复，删；
   若没有渲染，它是在告诉用户"这份索引覆盖三类东西"，属合法信息——两种情况必须给出屏幕证据，不能靠句式推定。
2. **禁止"意图说明"句式**：`默认…`、`…即可`、`…一条不少`、`…不言自明` 这类以解释为目的的收尾，进禁止词表。
3. **状态句改标记**：`本页只读` 这类状态描述进禁止词表；状态要用界面标记（Tag/Badge）表达。

每条禁止项都要**注明它来自哪次真实反馈**（删掉的原文 + 日期），这样后面的人知道为什么它被禁，
而不是把它当成一条凭空的洁癖。
**范围**：这套标准管 **UI**。CLI（`dshpmc`）是纯文本界面，**没有标记可用**——它的「状态句」是唯一表达形态，
不适用「状态要改成标记」那一条；`--help` 里的 `(read-only)` / `(exit 1)` 这类括注是**帮助信息**，保留；
命令输出里的「你接下来可以这样装」是**可执行出路**，保留。
反过来，**纯粹的背景介绍与教学句在 CLI 里同样要删**——判断标准与 UI 一致：删掉它用户还能不能完成任务。
### 12.5 删信息的人，负责证明它已在别处可被看见（2026-09-19）

删一句"重复屏幕已有信息"的文案时，**删除的合法性依赖那个前提**：屏幕上真的已经有它了。
所以删的人必须同时做到两件事，否则这次删除是**把一个事实从界面上拿掉**，而不是去掉冗余：

1. 说清**屏幕上哪里已经有了**（哪个元素、哪一行、哪个 Tag）；
2. **让测试钉住那个替代载体**——不能只把断言的字面跟着文案一起放松。

实例（本轮真实发生）：`health.grouped` 的括注被删，理由是"计数由组头 `health.groupCount` 承担"；
那么断言除了检查新的总结句，**还必须检查组头那串计数真的渲染了**。
`health.foreignTitle` 的「（不是当前环境）」被删，理由是"由「非当前环境」Tag 表达"；
那么断言就必须检查那个 Tag 真的在。

反模式：把断言从「…（共 173 条」改成「…」，然后什么都没补——测试绿了，保证没了。

### 12.6 host 侧输出文案（第三次反馈，2026-09-19）

**用户反馈原文**：*"我们的现在的UI文案已经严重违反我们的要求了，极其不通顺与大量冗余"*，附实拍。
问题文本逐字：

```
安装失败：无法试装（不算通过），回滚失败（not-removable）dsh-probe-block：无法试装：候选（dsh-probe-block）
装进了 node_modules，但**没有进入组合层栈**（dsh.profile.bundles = ["@deepseek-ai/dsh-base",
"@deepseek-ai/dsh-web-app","dsh-plugin-manager-companion"]）——挂载期不会加载它。原因：它在本轮之前
就已经是测试环境的依赖，官方 reconcile 会跳过"既有的"依赖。这不等于通过。package.json：依赖声明仍在，
层栈已不含 dsh-probe-block。node_modules：仍留有 dsh-probe-block 的目录。需要时请手工删除它。
```

§12.2–§12.5 管的是**客户端字典**（一句话删不删）；这一段管**host 拼出来的输出**——它是另一类问题：
每条信息单独看都"如实"，**叠在一起**就成了没人读得完的东西。三类：

| 类 | 实例（实拍原文） | 为什么不行 | 正确形态 |
|---|---|---|---|
| **三层拼接** | `安装失败：…，回滚失败（…）包名：` ＋ 守卫 detail ＋ 回滚状态尾 | 结论、回滚状态、包名三个状态用逗号硬拼成一行，读者要自己拆句子 | **分层**：第一行给结论与后果，细节降到下面；一次只说一件事 |
| **内部术语上屏** | `dsh.profile.bundles` / `reconcile` / `not-removable` / `node_modules：` | 实现词汇。用户要的是"发生了什么、我该怎么办" | 翻成人话（"启动列表里没有它" / "重复安装不会生效" / "官方不允许移除这个组合包"）。官方错误码只留在结果对象的 `error` 字段里供排查 |
| **字面 `**`** | `但**没有进入组合层栈**` | web 上是**可见星号**（§3.6 陷阱 #1，task-55/71 已清过客户端一批） | 直接陈述，或用「」；本仓库一律不用 markdown 强调 |
| **路径语义当主语** | `package.json：依赖声明仍在，层栈已不含…` / `node_modules：仍留有…` | 把文件名当句子主语，用户读到的是我们的目录结构 | 说结果："依赖声明还在" / "安装目录里还留着本次安装的残留" |

**改写示例（同一场景，改前 → 改后）**：

```
改前：安装失败：无法试装（不算通过），回滚失败（not-removable）dsh-probe-block：无法试装：候选（…）装进了
      node_modules，但**没有进入组合层栈**（dsh.profile.bundles = […]）——挂载期不会加载它。原因：…
      官方 reconcile 会跳过"既有的"依赖。… package.json：依赖声明仍在，层栈已不含… node_modules：仍留有…

改后：没有安装 dsh-probe-bad：候选包导致挂载失败。
      回滚没有完成（官方不允许移除这个组合包）。

      候选包导致挂载失败：dsh-probe-bad 装进 webprobe-dpmc 之后树挂不起来。
      根因链：
      Error: dsh: plugin tree failed to load: … duplicate loader entry id: probe-bad-row

      环境现状：
        依赖声明还在。
        安装目录里还留着它的文件（本次安装的残留），需要时可以手动删除。
```

**判据与 §12.3 一致**：删掉这句用户还能不能完成任务。能 → 删；不能 → 留，但**分层**且说人话。

### 12.7 host 侧护栏（`tests/ui-copy.test.mjs`）

§12.4 的护栏原本只扫 `src/client/locales.ts`，**host 侧输出零护栏**——于是 §12.6 那段
一路绿灯上了屏。所以护栏扩到 host 侧（`src/envManager.ts` / `src/index.ts` / `src/upgrade.ts`）：

| 规则 | 判据 | 反例（真实文案） |
|---|---|---|
| H1 字面 `**` | 字符串里出现 `**` | `但**没有进入组合层栈**` |
| H2 内部术语上屏 | 清单：`dsh.profile.bundles` / `reconcile` / `beforeDeps` / `dsh.bundle` / 官方错误码（`not-removable` 等）/ `node_modules：` | `（dsh.profile.bundles = […]）`、`回滚失败（not-removable）` |
| H3 一句堆三个状态 | **同一行**里出现「≥2 个冒号」且至少一个逗号 | `安装失败：无法试装（…），回滚失败（…）包名：…` |
| H4 渲染输出 | 驱动一次真实的安装失败，拿 `result.output` 过同一张表 | 抓 H1–H3 抓不到的**运行时拼接**（错误码是变量插进去的、分层是多行 `join` 的） |

**为什么 H4 必须存在**（变异验证发现）：源码扫描只看「含中文的字符串字面量」，于是两类改动会溜过去——
官方错误码是**变量插进模板**的（`'回滚失败（' + code + '）'`，字面量里没有 `not-removable`）；
分层输出是**多行数组 join** 的（任何单个字面量都不含拼接后的整段文本）。
判据因此前移到"用户真正看到的那个字符串"。

**提取器必须剥注释**：`src/envManager.ts` 的注释里合法地写着 `dsh.profile.bundles` / `reconcile`
（那是给维护者看的）。护栏要拦的是"发给用户的字符串"，不是"代码里提到过这个词"；
不剥注释，规则会退化成"不许在代码里提这个术语"——那是洁癖（§12.4）。

每条规则都带正反例，**反例取自上面那段真实文案**；8 处变异验证全部报红（把文案改回原样 → 护栏当场红）。

### 12.8 短文本不用句号（用户第四次反馈，2026-09-19）

用户原话：「我们可以尽量少用句号，特别在 UI 上非长端文本的时候。因为这时候的句号有种卡端的感觉，
我们日常所使用的很多软件的提示等不用句号也是这个原因。」

**规则（可判定，不是感觉）**：

| 场景 | 判据 | 处理 |
|---|---|---|
| 短文本 | 单句、无换行、**≤40 字**、整条只有一个句号 | **去掉结尾句号** |
| 长文本 / 多句 | 超过 40 字，或含换行，或句号 ≥2 个 | **保留句号**（多句之间没有分隔会读不动） |
| 引号/括号收尾的短句 | 先按长度判，与上同 | 短的去（`结论不是「通过」。` → `结论不是「通过」`） |

**边界（刻意保留句号的形态）**：
· 带分号的长说明：`满分 100；可自动修复扣 5 分…只报告扣 20 分。`（一条长句，去掉句号读不出收束）；
· 两句话的说明：`只删除本插件安装时落地的目录，且不可撤销。手工放置的目录不会被删除。`（**句号在这里是分隔符**，删了会粘成一句）；
· 引用的官方原文、错误码、命令示例：不动（那是别人说的话）。

**为什么是这条规则**（而不是"全部去句号"）：现状本来就不统一——714 条字典里 **240 条短文本本来就没句号（34%）**，
48 条带句号，是少数派。短提示带句号会读成"断句"，而长说明不带句号会读成"一句话没说完"。规则要能同时照顾两头，
否则改完还会漂回去。

**护栏**：`tests/ui-copy.test.mjs` 增加规则 P1——**短文本结尾不得有句号**（判据同上表第一行）；
长文本不受影响（正例必须放行，否则规则会退化成"不许有句号"）。


**本轮落地（2026-09-19，host2）**：

| 范围 | 结果 |
|---|---|
| 客户端字典 zh | 53 条带句号 → 48 条短文本去句号，5 条保留（两句或长句） |
| 客户端字典 en | 与 zh 同步；**阈值取 60 字符**（理由见下） |
| host 侧（`envManager` / `index` / `upgrade`） | 短句逐条去句号；拼接片段按「用户看到的那一行」判 |

**英文阈值为什么是 60 而不是 40**：中英字数不是一个量纲——中文 40 字已说完一句话，
英文 40 个字符常常只是半句。实测本仓库英文语料的分带句号占比：

| 长度带 | 有句号的比例 |
|---|---|
| 0–40 字符 | 5% |
| 41–60 字符 | 36% |
| 61–80 字符 | 83% |
| 81–100 字符 | 100% |

规范在 60 附近翻转（≤60 里 93% 不带句号），所以英文取 60。两档都来自**现有语料的多数派**，
不是新立的规矩；中文侧 ≤40 字里 86% 不带句号，与本节表格写的 40 一致。

**§12.8 是两层标准，不是一条阈值**：第一层是上表的机械判据；第二层是「边界」点名的例外。
`health.scoreHint`（`满分 100；…只报告扣 20 分。`）**长度像短句但必须保留句号**——
它是一条带分号的长说明，去掉句号读不出收束。护栏因此把它单列为显式例外，
并单独断言「它确实是短文本」（证明例外真的在起作用，而不是碰巧没被扫到）。

**host 侧护栏的实现难点（`tests/ui-copy.test.mjs` 规则 P1）**：判据单位必须是
**用户看到的那一行**，而不是单个字符串字面量。两种写法渲染结果不同：

- `'这个进程不是以某个环境启动的，' + '所以无法定位该环境的安装位置，跨环境的包操作做不了。'`
  → **一行**（42 字，句号保留）。逐字面量判会把 26 字的第二段误认成短句而删掉分隔句号；
- `'没有安装 ' + name + '：扫描没能完成'`（数组元素）→ **一行**；
- `'甲' + expr + '。'` → 一行，但那个孤立的 `'。'` 字面量**不含中文**，
  只按「含中文」过滤会整条丢掉，短句就带着句号留在界面上。

所以 P1 按**拼接链**判：只用 `+` 相连的算一行；遇到顶层逗号（数组元素）、三元 `?`/`:`
（互斥分支）、`;`/`}`（语句/块边界）就断开。动态部分折叠成占位符，既不影响长度也不影响句号计数。
孤句号写法（静态判不出整行多长）必须进 `PERIOD_APPEND_ALLOWED` 显式登记并写明理由——
**没登记就是失败**：宁可要人看一眼，也不让「无法判定」静默变成「没问题」。

**落地后的裁决记录（Lead，2026-09-19，task-86 复核）**：

1. **`health.scoreHint` 保留句号——维持**。它在界面上是 **Tooltip 内容**（`ConsolePage.tsx:559`），
   形态是带分号的三段式长说明（`满分 100；可自动修复扣 5 分，需确认扣 10 分，只报告扣 20 分。`）。
   长度像短句、形态是长说明 —— 属于第二层标准点名的例外，护栏已把它列为显式例外并单独断言"它确实是短文本"。
2. **英文阈值 60 维持**。改前基线（host2 实测）与改后实测（Lead 复核）互补印证：
   · 改前：0–40 字符 5%、41–60 36%、61–80 83%、81–100 100% —— 翻转点在 60 附近；
   · 改后：≤60 字符 **0%** 带句号、61–80 **83%**、81+ **100%** —— 规则已精确生效，长句未误伤。

### 12.9 结果块的行文结构（用户第五次反馈，2026-09-19）

用户对着"升级结果"块逐项指出问题。**这一节的规则都来自真实截图**，每条都点名了反例。

**R1 · 结论词紧随主语，不要被别的词隔开。**
反例：`已升级 @deepseek-ai/dsh-experimental-auto-review（这次没有验证）` ——
"已升级"与"未验证"这两个结论被包名隔开，读者要跳读才能把两件事连起来。
改：`@deepseek-ai/dsh-experimental-auto-review 已升级（未验证）`。
附带：`（这次没有验证）`→`（未验证）`。"这次"是废话——结果块本身就是一次升级的结果。

**R2 · 原因必须**归属**，不许冒号套冒号。**
反例（用户原话："并行与分句，让人的理解很困难"）：
`原因：试装总开关已关闭：未做金丝雀，直接升级（没有验证新版本能否挂载）`
—— 一层冒号又一层冒号再套括号，读者要拆三遍。
改：**缩进或树状**，一层一个因果：

```
原因：
  试装总开关已关闭
    → 直接升级，没有先验证
      → 新版本能否加载未经验证
```

**R3 · 禁止内部代号上屏。**（与 §12.6 的 H2 同类，但清单不同）
本轮点名：**金丝雀**（内部代号，中文用户第一反应是"什么鸟"）、**盘上事实**（on-disk facts 的硬译）、
**挂载 / 快照 / 层栈 / 锚点**（内部机制词）。要换成用户语言：
| 代号 | 用户语言 |
|---|---|
| 金丝雀 | 试装验证 / 先验证一遍（按上下文） |
| 盘上事实 | 当前状态 |
| 挂载 / 挂不起来 | 加载 / 起不来 |
| 快照 | 环境副本（必要时加一句它是什么） |

**R4 · 结论只留一处，原因只讲"为什么"。**
反例：标签写 `未验证`，下面又写 `（没有验证新版本能否挂载）` —— 同一件事说两遍。
按 §12.5：结论已由标签承载时，下面只说**原因**。

**R5 · 原始日志必须有标识。**
反例：把 pnpm 原始输出（`[WARN] Issues with peer dependencies…`）直接贴出来 ——
中英混杂、没有出处，读者会以为那是我们的说明。要加一行标识（如 `命令输出`）+ 说明它来自哪条命令。

**R6 · 历史记录必须交代时间性。**
反例：`上次升级结果`（"上次"意味不明、有误导性）；改 `最近一次升级`（明确这是历史记录，不是当前状态）。

**R7 · 产品文案不口语化。**（用户原话："我们不是在与用户聊天"）
禁：`了` 收尾的闲聊语气、`吧/呢/哦`、`一下`、`帮你`、第二人称闲聊（`你可以…`）。
说明性文字用陈述句。

**落地要求**：本轮先把这 7 条落成可判定护栏（能机械判的机械判，不能的建清单 + 点名反例），
再改文案。**host 侧运行时拼接的文案必须纳入**——本轮实测：`金丝雀` 在客户端字典里 0 条、
在 host 侧 11 处，且它是**运行时拼出来的**，只扫字面量的护栏抓不到（§12.7 的 H4 教训：判据要落在
"用户真正看到的那个字符串"上）。

3. **覆盖范围如实登记**：本轮纳入 `src/client/locales.ts`（zh+en）与三个 host 源文件
   （`envManager.ts` / `index.ts` / `upgrade.ts`）。**未纳入**：CLI 帮助文本（§12.3.1 已定 CLI 不做标记化，
   它是文本界面）、`tools/` 与 `docs/` 里的说明文字。别把"已纳入"读成"全仓覆盖"。
### 12.10 断言钉在哪：按硬度排序（2026-09-20，从 task-88~94 的六次事故里提炼）

这一节不属于"文案标准"，它是**总纪律"代理 ≠ 事实"在测试层的具体化**——写在这里是因为它是由
一连串**文案相关的验证事故**逼出来的（§12.7 的 H4、§12.8 的 P1、§12.9 的 R1–R7 都依赖它）。

**排序（越靠前越硬，优先用它；只有拿不到前面的，才退到后面的）**：

| 硬度 | 钉什么 | 例 |
|---|---|---|
| ① **副作用本身** | 动作真的发生了：op 请求记账、文件真的被删、目录列表变化、盘上版本变化 | 「点确认后 trialCleanup op 真的发出去了」 |
| ② **只在动作后出现的结构** | data-* 状态、结果块节点、渲染出的行边界 | 「按标签边界切出的屏幕行里，没有一行含两个冒号」 |
| ③ **文案** | 只在①②拿不到时用，**且要挑"只在动作后出现"的那一句** | 结果行的「已清理 N 个」 |

**三次事故（都是"退到③"或"用①的替代品"造成的）**：

1. **用①的替代品代替①**（task-90）：变异脚本 `>/dev/null 2>&1` 丢掉构建退出码 → 构建失败被当成成功 →
   dist 保持旧内容 → 测试跑的是**未变异的产物**，却报"变异没被抓住"。**"脚本没报错"不是"变异生效了"。**
2. **退到③导致恒真**（task-92）：断言"面板里有『没有需要清理的测试环境』"——**那句话点之前就在了**
   （空计划时计划区说的就是它）。点没点都绿。
3. **③ 与被断言的东西绑太紧**（task-93）：数"某句措辞出现几次"，产品一改措辞计数就恒为 1。

**同族反面教材（控件定位）**：触发按钮与确认按钮**同名**（都叫「清理过期」）时，按文本取"第一个可见的"
会点到错的那个——**按文本定位控件必须限定范围**（容器、角色、位置），否则 ① 的位置信息被同名文案污染。

**一条推论**：当"动作是否发生"与"屏幕内容是否为空"在 HTML 上同形时（"那块没渲染" vs "那块内容为空"），
渲染级断言**必然静默通过**。这类跨模块拼接的契约要用**源码级断言**钉（从两侧源码各取常量断言逐字相等）。
**推论二（task-97 追加）：判据范围必须按规则自己的定义选。**"这一行"与"这一块"是两个不同的判据单位：
· R5 判"这一块里有没有那行标识"（标识与原文是两个相邻元素），按行判会**假阳性**；
· R6 判"这一行有没有时间词"，按块判会**假阳性**（另一处合法地出现同一个词）。
选错单位 = 判据与规则说的不是同一件事。

**推论三（task-97 追加）：SSR 桩件比真机宽松，两个方向都会漏。**本轮实测两例——
官方 Modal 桩即便 `open=false` 也渲染 children（"去掉二次确认"在 SSR 上看不出差别）；
`.panel { display: flex }` 把 HTML 的 `hidden` 压掉（断言全绿而图上两个面板叠着，且 hidden 子树 `innerText` 照样读得到）。
这类"跨不过渲染"的接线，用**源码级断言**钉。


## 13. 依赖兜底目录的陈旧链接（2026-09-20，用户反馈 + 实测）

**用语纪律（用户 2026-09-20 指出）**：这个目录**不许用用户的自称来称呼**——那个词既不是本项目用语，
也不是官方用语，本仓库文档/注释/issue code/用户可见文案里一律不出现。
官方用语是 **module fallback**（`healProfilesModuleFallback`）；本项目文档用**依赖兜底目录**，
必要时括注路径 `$DSH_HOME/profiles/node_modules`。代码标识符沿用官方语义（`moduleFallback*`）。

### 13.1 机制（实测核对，不是推测）

`$DSH_HOME/profiles/node_modules` 是官方为所有 profile 铺的**共享依赖兜底层**。两个事实叠起来产生残骸：

1. **只增不删**：判据 `moduleFallbackCurrent = entries.every(...)` 只问「该有的到位没」，
   不问「有没有多出来的」；写入路径 `healProfilesModuleFallbackLocked` 只遍历 `entries`，
   **没有 readdir、没有 unlink**。上游测试把它钉成契约（`retains current links while repairing a missing sibling`）。
2. **0.1.6-alpha.2 默认根本不写它**：默认 `resolutionMode = "runtime"`（`apps/cli/src/profile-boot.ts:263`），
   runtime 走 `createProfileResolutionGeneration` → `materialize: false`。
   用全新临时 HOME 起实例实测：链接数 **0**。

⇒ 现存链接是 **≤0.1.5 时代的残骸**，且**已不参与解析**。这不是解析缺陷，是磁盘上的过期产物。
### 13.1.1 计数口径（Lead 复核发现两个数都对、含义不同，必须写清）

**本模块（以及任何引用这些数字的地方）一律按「扫到的那一层」计数**：

| 口径 | 命令 | 真机实测 |
|---|---|---|
| **顶层链接** | `find . -mindepth 1 -maxdepth 1 -type l` | **171** |
| **`@scope/` 下一层链接** | `find . -mindepth 2 -maxdepth 2 -type l` | **345** |
| **本模块的口径 = 两者之和** | 顶层链接 + scope 下一层 | **516** |
| 顶层条目总数（链接 + 目录） | `find . -mindepth 1 -maxdepth 1` | 200（171 链接 + 29 个 `@scope` 目录）|

**两个数都对，含义不同**：171 只数顶层，516 把 `@scope/` 里那一层也算上。
本模块扫的是**两层**——顶层链接，加上 `@scope` 目录**里**的链接（只下探这一层，不递归）。
所以模块报出来的计数是 **516**，不是 171。

⚠ 任何地方引用这个数字**必须带口径**。只写「516 条」而不说「含 scope 下一层」会让后来人
用 `maxdepth 1` 去核，得到 171，然后以为文档写错了。

真机实测（2026-09-20）：516 条符号链接（**口径 = 顶层 171 + `@scope/` 下一层 345**），
**全部断链**，目录 mtime 停在 9 月 15 日。


### 13.3 真机清理的最终验收（2026-09-20，Lead 执行）

功能落地后，在**用户真机**上执行了一次实际清理，作为这条功能的端到端证据：

| 项 | 值 |
|---|---|
| 清理前 | **516** 条链接（口径 = 顶层 171 + `@scope/` 下一层 345），**全部断链** |
| 执行方式 | `scanModuleFallback` + `cleanupDanglingLinks`（与 REST `fix` op 同一逻辑）|
| 清理后 | 链接 **0** 条、断链 **0** 条 |
| 幂等复核 | 二次扫描 `total = 0`（不重复报、不重复删）|
| 清理后启动 | 用真实 `web` profile 起实例：正常挂载、输出就绪行、**零失败特征** |

**两点如实记录**：
1. **执行时的两次失误**：第一次走 REST op 返回空（3080 实例有官方 browser-auth，没有 token）；
   第二次**猜了函数签名**（`scanModuleFallback(dir, closure)` 传成对象、漏了 `cleanupDanglingLinks` 的 `remove` 注入参数）→
   `TypeError`。读源码后按真实签名重跑才成功。**两次都是"用猜测代替事实"——这条纪律我们自己也会犯。**
2. **清理留下的空目录**：删完链接后留下 **29 个空的 `@scope/` 目录**（链接删了、装链接的目录还在）。
   无害（Node 解析找不到包名就跳过，官方 lister 也只认符号链接），但用户看着仍是"目录里有东西"。
   已按同一理由（废弃产物该清干净）补一条：清理时顺带 `rmdir` **变空的** scope 目录，**只删空目录**。

### 13.2 三分类与处置（用户裁决）

| 类别 | 判据 | 处置 |
|---|---|---|
| **断链** | `lstatSync().isSymbolicLink()` 为真但目标不存在 | **默认自动删**（旧时代产物，管理器该做的事）|
| **完好但过时** | 目标存在，但不在**当前安装闭包**里 | **报但不删**（用户要知道在哪，可手动管）|
| **正常** | 目标存在且在闭包里 | 不动 |

闭包从 `healProfilesModuleFallback({ installAnchor, materialize: false })` 取——**复用官方实现**，
不自己复刻依赖遍历（复刻就会与官方漂移）。

⚠ **锚点必须先 `realpath`**：传 `node_modules/@deepseek-ai/dsh/package.json` 这种符号链接路径时，
官方用 `createRequire(anchor).resolve.paths` 逐层向上找，从链接所在层开始会找不到兄弟依赖，
闭包只剩锚点自己那一条（**实测 463 → 1**）。这是静默错，有专门测试钉住。

### 13.3 清理纪律（三条，缺一不可）

1. **只删符号链接**：每条都先 `lstatSync().isSymbolicLink()` 确认（`lstat` 不跟随链接，
   确认的是「这个条目本身是链接」）。**绝不递归删目录**——即使目录看起来是空的。
2. **逐条重核**：扫描到删除之间目录可能被别的进程动过，删除前**重新**判一次断链；
   目标恢复了就跳过并说明。不做「按扫描结果批量 rm」。
3. **结果如实**：删了几条 / 跳过几条 / 为什么跳过 / 哪几条失败，逐条报出来。

### 13.4 私有层（`<profile>/.dsh-module-fallback`）——官方漏了

共享层只增不删，但**私有层官方有反向差集清理**（`healProfileModuleFallback` 里的
`for (const packageName of ownedPackageNames(ownedModulesDir)) if (!links.has(packageName)) removeProfileSymlink(...)`）。
所以私有层出现断链是**官方没清掉**，属于另一类问题。

真机实测（2026-09-20）：`~/.dsh/profiles/web/.dsh-module-fallback/node_modules` 里有 **2 条断链**
（`react`、`loose-envify`，都指向已不存在的 `<profile>/node_modules/<name>`），官方清理没有移除它们。
同类另有 3 条（`@deepseek-ai/dsh-experimental-*`）目标都在，正常。

**处置**：单独报一条 `profile-module-fallback-dangling-link`（`report-only`，**没有 fix**）——
这个目录归官方管，本项目只如实报出来，不代它清理。

### 13.5 护栏与实现要点

`tests/moduleFallback.test.mjs`（22 例）+ `src/moduleFallback.ts`。几处容易写错、已用测试钉住的点：

- **共享层与私有层各自独立**：共享层目录不存在（或闭包算不出来）**不该连累私有层**。
  真机 `web` profile 正是这个形态：共享层不存在，私有层却有 2 条断链。
- **闭包算不出来时断链照样查**：断链的判据是「目标在不在」，与闭包无关。
  整份返回空会把**能查的断链一起丢掉**——而断链正是要自动清理的那一类。
  此时只把「过时」这一类标成没查，且**不带 `layers`**（带了会被界面画成整层「没查」，
  把已经查出来的断链一起抹掉）。
- **只下探 scope 一层**：`@scope` 本身是真目录（官方 `mkdirSync(dirname(link))` 建的），
  不是链接。不递归——多下一层就可能扫到并删掉不该碰的东西。
- **开关只静音「过时」**：`diagnostics.reportStaleModuleFallbackLinks`（默认 true）。
  **断链不受它管**——断链默认自动删，不该被一个提示开关顺手关掉。
